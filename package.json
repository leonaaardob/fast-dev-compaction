{
  "name": "fast-jev-compaction",
  "version": "0.3.0",
  "description": "Verbatim Jev-guided context restoration around Codex compaction.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "hooks",
    "skills",
    "plugin.json",
    ".codex-plugin",
    ".agents",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "check:hooks": "node --check hooks/pre-compact.mjs && node --check hooks/session-start.mjs && node --check hooks/post-compact.mjs && node --check hooks/cli.mjs && node --check hooks/lib/rollout.mjs && node --check hooks/lib/render.mjs && node --check hooks/lib/state.mjs && node --check hooks/lib/config.mjs",
    "test": "vitest run",
    "demo": "tsx examples/demo.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/leonaaardob/fast-dev-compaction.git"
  }
}
